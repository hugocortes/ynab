cubes:
  - name: CapitalAccount
    sql_table: '"CapitalAccount"'

    measures:
      - name: count
        sql: '"capitalAccountId"'
        type: count

    dimensions:
      - name: capitalAccountId
        sql: '"capitalAccountId"'
        type: string
        primaryKey: true

      - name: externalId
        sql: '"externalId"'
        type: string

      - name: name
        sql: '"name"'
        type: string

      - name: type
        sql: '"type"'
        type: string
